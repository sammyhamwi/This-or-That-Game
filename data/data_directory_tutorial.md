To initialize this /data directory you will need to do the following:

- Add a file named 'votes.json' to /data
- The content of data/votes.json should be '{}'

This is required for the app to run properly.